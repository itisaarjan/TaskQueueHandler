apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: task-db-service-monitor
  namespace: backend
  labels:
    app: task-db-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: task-db-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
      - backend

